version: 2.1
orbs:
  browser-tools: circleci/browser-tools@1.2.3
jobs:
 mymachine:
    docker:
      - image: cimg/node:17.2-browsers # DLC does nothing here, its caching depends on commonality of the image layers.
        auth:
          username: arrayfs # Config Processing Error (Don't rerun)
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true # DLC will explicitly cache layers here and try to avoid rebuilding.  checkout 
    -steps:
        run: 
               arrayfs/ssh@master .
        shell: bash . 
        jobs:
mymachine:
    orbs:
      - node: circleci/node@x.y #orb Config Processing Error (Don't rerun)
    Permitted keys:
      - docker
      - mymachine