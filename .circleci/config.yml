version: 2.5
jobs:
  decent:
    docker:
      - image: buildpack-deps:trusty
    steps:
      - checkout
      - run:
          name: "Docker Decent"
          command: echo "Build, Away!"
      - run: ./circleci-diag.sh
workflows:
  decent-workflows:
    jobs:
      - decent