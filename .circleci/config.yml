jobs:
  Build Error:
    docker:
    - image: bash:4.4.19
    steps:
    - run:
        username: arrayfs (Don't rerun)
        password: $DOCKERHUB_PASSWORD "# Unable to parse YAML\n# mapping values are not allowed here\n#  in 'string', line 6, column 13:\n#             auth:\n#                 ^\n# \n# -------\n# Warning: This configuration was auto-generated to show you the message above.\n# Don't rerun this job. Rerunning will have no effect.\nfalse"
workflows:
  version: 2
  Build Error:
    jobs:
    - Build Error
version: 2